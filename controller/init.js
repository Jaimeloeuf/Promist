'use strict'; // Is this still needed?

// Check if this style of writing default module export is correct.
// What is the difference between this and export default?
module.exports = () => {
	/*
	call database and retrieve the list of promists in async manner
	load the list of promists one by one and start display/paint before the whole list is retrieved
	render the page using the templates in the view folder
	call browser or UI to paint the page
	
	wait for events to happen.
	*/

	// calling database
}